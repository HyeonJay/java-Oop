package encapsule;

public class AverageVOStep1
{
	String name;
	int kor;
	int eng;
	int tot;
	double avg;
	/*
	 * 앞서 절차지향에서 코딩을 하다가 프로그래머들이 규칙을 발견한다. 
	 * 사람이름 + 과목명 으로 변수명을 준다. 근데 과목명은 반복이 된다. 
	 * 따라서 반복되는 과목명을 따로 분리해서 클래스를 만든다.
	 * 
	 * 이때, 클래스 바로 밑에( 즉 메소드 영역이 아닌 )
	 * 클래스 영역에 선언된 변수를 인스턴스 변수라고 한다.
	 * 그리고 인스턴스 변수가 선언된 영역을 "필드" 라고 한다.
	 * 그려면 오더를 받을 때 "필드에 변수를 선언해라!?.."라고 받으면
	 * 지역변수가 아닌 공용으로 쓸 인스턴스 변수를 선언하라는 뜻이 된다.
	 */
}
